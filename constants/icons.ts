import {
  Box,
  ChartNoAxesColumn,
  FileText,
  House,
  Package,
  Settings,
} from "lucide-react-native";

export const icons = {
  home: House,
  analytics: ChartNoAxesColumn,
  products: Package,
  invoices: FileText,
  settings: Settings,
  box: Box,
} as const;

//export icons;
