import {
  ChartNoAxesColumn,
  FileText,
  House,
  Package,
  Settings,
} from "lucide-react-native";

export const icons = {
  home: House,
  analytics: ChartNoAxesColumn,
  products: Package,
  invoices: FileText,
  settings: Settings,
} as const;

//export icons;
